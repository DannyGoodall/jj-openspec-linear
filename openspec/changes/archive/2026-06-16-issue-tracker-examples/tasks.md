# Tasks: issue-tracker-examples

## 1. Documentation contract

- [x] 1.1 Amend the `documentation` capability's **Issue-tracker interaction
  doc** requirement to mandate a worked-example gallery of complete
  `docs/agents/issue-tracker.md` configurations, each marked as commentary.

## 2. Author the gallery

- [x] 2.1 Add the gallery to `docs/issue-tracker.md` with five fenced, captioned
  examples: Linear pinned; GitHub pinned; `Mode: none`; Linear MCP present but
  pinned `github` (anti-hijack); minimal one-line Linear.
- [x] 2.2 Mark the gallery unmistakably as commentary — not the consuming repo's
  live config — and explain that the live config is a single file scaffolded
  from the template.
- [x] 2.3 Link to the gallery from `docs/optional-components.md`.

## 3. Version

- [x] 3.1 Bump `plugins/change-lifecycle/.claude-plugin/plugin.json` `0.1.2` →
  `0.1.3`.
